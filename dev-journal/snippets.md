# Snippets


## Note 819211

```ts
export const clamp = (n: number, a: number, b: number) => Math.max(a, Math.min(b, n));
```

## Note 210635

```ts
export const clamp = (n: number, a: number, b: number) => Math.max(a, Math.min(b, n));
```

## Note 423334

```ts
export const clamp = (n: number, a: number, b: number) => Math.max(a, Math.min(b, n));
```

## Note 961973

```ts
export const clamp = (n: number, a: number, b: number) => Math.max(a, Math.min(b, n));
```

## Note 279146

```ts
export const clamp = (n: number, a: number, b: number) => Math.max(a, Math.min(b, n));
```

## Note 179354

```ts
export const clamp = (n: number, a: number, b: number) => Math.max(a, Math.min(b, n));
```
